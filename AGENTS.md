# AGENTS.md

## 1. 프로젝트 개요

이 저장소는 활동지원사가 현장에서 필요한 업무 정보를 빠르게 확인하기 위한 **모바일 우선 업무매뉴얼 PWA**이다.

주요 사용 환경은 데스크톱보다 스마트폰이며, 디자인의 화려함보다 다음을 우선한다.

1. 필요한 정보를 빨리 찾을 수 있을 것
2. 글자가 충분히 크고 읽기 쉬울 것
3. 버튼과 링크를 쉽게 누를 수 있을 것
4. 사용자가 길을 잃지 않을 것
5. 기존 업무 정보와 외부 자료 연결이 정확할 것
6. 기존에 정상 작동하던 기능을 깨뜨리지 않을 것

이 프로젝트는 일반적인 서비스형 웹앱보다 **현장 업무매뉴얼**의 성격이 강하다.

따라서 개발 편의나 코드 미관을 이유로 사용자 경험이나 기존 콘텐츠 구조를 임의로 변경하지 않는다.

---

# 2. 기술 스택

현재 프로젝트의 기본 기술 스택은 다음과 같다.

- Vite
- React
- TypeScript
- React Router
- Tailwind CSS
- shadcn/ui
- Radix UI
- Lucide Icons
- TanStack React Query
- vite-plugin-pwa
- GitHub Pages
- GitHub Actions

기존 기술 스택을 우선 사용한다.

명확한 필요성이 없는 경우 새로운 프레임워크, UI 라이브러리, 상태관리 라이브러리, CSS 시스템 또는 대규모 의존성을 추가하지 않는다.

---

# 3. 가장 중요한 작업 원칙

## 최소 수정 원칙

문제를 해결하는 데 필요한 **가장 작은 범위만 수정한다.**

작업 요청과 관계없는 코드를 정리하거나 리팩터링하지 않는다.

다음 행동을 특히 금지한다.

- 요청하지 않은 대규모 리팩터링
- 전체 디자인 개편
- 프로젝트 구조 재편
- 폴더 구조 변경
- 파일명 일괄 변경
- 기존 컴포넌트의 불필요한 재작성
- 정상 작동하는 코드의 스타일 통일 목적 수정
- 요청과 관계없는 dead code 정리
- 패키지의 임의 추가 또는 제거
- 콘텐츠의 임의 이동
- 외부 링크의 임의 변경
- 기존 기능의 임의 삭제

"더 깔끔해 보인다"는 이유만으로 구조를 변경하지 않는다.

---

# 4. 변경 전 반드시 확인할 것

코드를 수정하기 전에 먼저 실제 저장소 상태를 확인한다.

추측만으로 수정하지 않는다.

최소한 다음을 확인한다.

1. 관련 파일
2. 관련 컴포넌트
3. 관련 hook
4. 현재 import 관계
5. 현재 콘텐츠가 저장된 위치
6. 관련 라우팅
7. 관련 정적 파일
8. package.json scripts
9. 변경이 PWA 또는 GitHub Pages 배포에 영향을 주는지

사용자가 특정 문제를 요청했다면 먼저 그 문제가 발생하는 실제 코드 경로를 찾는다.

원인을 확인하지 않고 여러 파일을 동시에 수정하지 않는다.

---

# 5. 프로젝트 구조

현재 주요 구조는 다음 원칙을 따른다.

```text
src/
├─ assets/
├─ components/
│  ├─ ui/
│  └─ ...
├─ hooks/
├─ lib/
├─ pages/
│  ├─ Index.tsx
│  └─ NotFound.tsx
├─ App.tsx
├─ App.css
├─ index.css
└─ main.tsx

public/
├─ documents/
└─ ...
```

기존 구조를 가능한 한 유지한다.

---

# 6. 파일별 역할

## src/pages

페이지 단위 UI를 둔다.

현재 핵심 페이지는:

```text
src/pages/Index.tsx
```

이다.

이 파일에는 활동지원사 업무매뉴얼의 실제 콘텐츠가 상당 부분 포함되어 있다.

따라서 Index.tsx를 단순한 레이아웃 파일로 간주해서는 안 된다.

---

## src/components/ui

shadcn/ui 및 공통 UI primitive 영역이다.

특별한 이유 없이 이 영역의 기본 컴포넌트를 대규모 수정하지 않는다.

앱 전용 기능을 `components/ui` 안에 무분별하게 넣지 않는다.

---

## src/components

앱에서 사용하는 재사용 가능한 기능성 컴포넌트를 둔다.

예:

- CanvaViewer
- FloatingNav
- InstallPrompt
- NavLink

기존 컴포넌트로 해결할 수 있는 기능이라면 새 컴포넌트를 만들기 전에 기존 구현을 확인한다.

---

## src/hooks

재사용되는 상태 또는 동작 로직을 둔다.

예:

- useFontSize
- usePWAInstall
- use-mobile
- use-toast

컴포넌트 내부에 이미 존재하는 간단한 로직을 이유 없이 hook으로 분리하지 않는다.

반대로 여러 곳에서 반복되는 상태 로직이라면 기존 hook 패턴을 우선 검토한다.

---

## src/assets

앱 빌드 과정에서 import하여 사용하는 이미지 등의 자산을 둔다.

---

## public/documents

사용자에게 직접 제공되는 PDF 등의 정적 문서를 둔다.

기존 문서 경로는 외부 또는 내부 링크에서 사용될 수 있으므로 파일명이나 경로를 임의로 변경하지 않는다.

---

# 7. Index.tsx 보호 규칙

`src/pages/Index.tsx`는 현재 프로젝트의 핵심 파일이다.

많은 실제 업무 콘텐츠가 JSX에 직접 포함되어 있다.

따라서 수정할 때 특히 주의한다.

특정 섹션 수정 요청을 받았다면 가능하면 해당 섹션만 수정한다.

다른 Accordion, 링크, 안내문, 연락처, 외부 콘텐츠를 동시에 정리하지 않는다.

단순 코드 정리를 목적으로 전체 파일을 다시 작성하지 않는다.

대규모 component extraction도 요청이나 명확한 필요성이 없다면 수행하지 않는다.

파일이 길다는 이유만으로 임의 분리하지 않는다.

---

# 8. 콘텐츠 보호 규칙

이 프로젝트에서 콘텐츠는 단순한 샘플 데이터가 아니다.

실제 업무에 사용되는 정보일 수 있다.

따라서 다음 내용을 임의로 수정하지 않는다.

- 전화번호
- 기관명
- 업무 안내 문구
- 지원 방법
- 공지사항 링크
- PDF 링크
- Canva 링크
- Notion 링크
- 카카오톡 링크
- 외부 웹사이트 링크
- 조합원 관련 안내
- 장애유형별 지원 정보

오탈자처럼 보이더라도 업무 의미가 바뀔 가능성이 있으면 임의 수정하지 않는다.

콘텐츠 정확성과 코드 스타일이 충돌하면 콘텐츠 보존을 우선한다.

---

# 9. 콘텐츠 위치를 임의로 변경하지 않는다

현재 콘텐츠는 크게 세 종류로 존재한다.

## 앱 내부 콘텐츠

주로 JSX/TSX 안에 직접 작성되어 있다.

## 정적 콘텐츠

예:

```text
public/documents/*.pdf
src/assets/*
```

## 외부 콘텐츠

예:

- Canva
- Notion
- Kakao
- 기타 외부 웹사이트

기존 콘텐츠를 다른 저장 방식으로 임의 이전하지 않는다.

예를 들어:

- JSX 콘텐츠를 JSON으로 전환
- Canva 콘텐츠를 앱 내부로 복사
- PDF를 iframe으로 변경
- 외부 링크를 다른 서비스로 교체
- 콘텐츠를 Markdown 기반 CMS 구조로 변경

등은 사용자의 명시적 요청 없이 수행하지 않는다.

---

# 10. UI 작업 원칙

기존 UI 시스템을 우선 사용한다.

우선순위는 다음과 같다.

1. 기존 프로젝트 컴포넌트
2. 기존 shadcn/ui 컴포넌트
3. Tailwind CSS
4. 기존 디자인 토큰
5. 필요한 경우에만 최소한의 새 구현

새로운 UI 프레임워크를 추가하지 않는다.

Material UI, Bootstrap 등 별도 UI 체계를 기존 shadcn/Tailwind 위에 추가하지 않는다.

---

# 11. 디자인 판단 기준

이 프로젝트에서는 다음 순서로 판단한다.

```text
사용성
>
가독성
>
접근성
>
모바일 편의성
>
일관성
>
시각적 화려함
```

디자인 개선 때문에 글씨가 작아지거나 버튼이 작아지거나 정보 접근 단계가 늘어나면 안 된다.

---

# 12. 모바일 우선

주요 사용 환경을 스마트폰으로 가정한다.

UI 변경 시 최소한 다음을 고려한다.

- 작은 화면
- 세로 화면
- 터치 조작
- 버튼 크기
- 링크 터치 영역
- 긴 텍스트 줄바꿈
- Accordion 사용성
- 고정 UI가 본문을 가리는지
- iframe 및 외부 콘텐츠의 모바일 표시

데스크톱에서만 보기 좋은 변경을 하지 않는다.

---

# 13. 글자 크기 기능 보호

프로젝트에는 `FontSizeProvider` 및 관련 기능이 존재한다.

글자 크기 조절은 접근성 기능으로 취급한다.

UI 변경 후 사용자 글자 크기 설정이 무시되지 않는지 확인한다.

고정 px 값 등을 과도하게 사용하여 글자 크기 조절 기능을 무력화하지 않는다.

---

# 14. 접근성

가능한 경우 기존 HTML semantic 구조를 유지한다.

특히 다음을 고려한다.

- 버튼은 실제 Button 요소 또는 기존 Button 컴포넌트 사용
- 링크는 목적이 명확할 것
- 아이콘만 있는 버튼은 접근 가능한 이름 제공
- 클릭 가능한 div 남용 금지
- 키보드 접근성을 불필요하게 깨뜨리지 않을 것
- 텍스트 대비를 지나치게 낮추지 않을 것
- 모바일 터치 영역을 지나치게 작게 만들지 않을 것

---

# 15. React Router 규칙

현재 React Router를 사용한다.

새로운 페이지 또는 route를 추가해야 하는 경우 catch-all route보다 위에 등록한다.

예:

```tsx
<Route path="/" element={<Index />} />

// 새로운 route는 여기에 추가

<Route path="*" element={<NotFound />} />
```

`*` route 아래에 새 route를 추가하지 않는다.

라우팅 변경 시 GitHub Pages의 직접 URL 접근 및 새로고침 동작도 고려한다.

---

# 16. PWA 보호 규칙

이 프로젝트는 PWA 설치 사용을 고려하고 있다.

관련 코드 또는 설정을 수정할 경우 다음을 확인한다.

- 홈 화면 설치
- standalone 실행
- manifest
- service worker
- 캐시
- 아이콘
- 새 버전 반영
- 기존 사용자에게 오래된 콘텐츠가 남을 가능성

PWA 문제와 관계없는 작업에서는 PWA 설정을 임의 변경하지 않는다.

---

# 17. 외부 콘텐츠 보호

CanvaViewer, Notion, Kakao 등의 외부 연결은 실제 운영 콘텐츠일 수 있다.

URL이나 embed 방식을 임의 변경하지 않는다.

외부 콘텐츠 문제를 수정할 때는 먼저 다음을 구분한다.

```text
앱 코드 문제
vs
외부 서비스 문제
vs
URL 문제
vs
iframe/embed 제한 문제
```

원인을 확인한 후 필요한 부분만 수정한다.

---

# 18. 의존성 관리

새 npm 패키지를 설치하기 전에 기존 의존성으로 해결 가능한지 확인한다.

기존 패키지로 해결 가능하면 새 패키지를 추가하지 않는다.

새 패키지가 반드시 필요한 경우 작업 보고에 다음을 명시한다.

- 추가한 패키지
- 추가 이유
- 기존 패키지로 해결할 수 없었던 이유

`package.json`을 변경하면 `package-lock.json` 일치 여부를 반드시 확인한다.

lockfile을 임의 삭제하거나 재생성하지 않는다.

패키지 전체 업그레이드는 별도 요청 없이 수행하지 않는다.

---

# 19. 설정 파일 보호

다음 파일은 프로젝트 전체에 영향을 줄 수 있으므로 필요하지 않으면 수정하지 않는다.

```text
package.json
package-lock.json
vite.config.ts
tailwind.config.ts
components.json
tsconfig*.json
eslint.config.js
.github/workflows/*
CNAME
```

특히 작은 UI 수정 때문에 위 설정 파일들을 함께 수정하지 않는다.

---

# 20. GitHub Pages 배포 보호

현재 배포는 GitHub Actions와 GitHub Pages를 사용한다.

main 브랜치 변경은 실제 서비스 배포와 연결될 수 있다고 가정한다.

따라서:

- 검증되지 않은 대규모 변경을 한 번에 만들지 않는다.
- deploy workflow를 임의 수정하지 않는다.
- CNAME을 임의 수정하거나 삭제하지 않는다.
- Vite base/path 설정을 이유 없이 변경하지 않는다.
- build 결과에 영향을 주는 설정 변경은 신중하게 수행한다.

---

# 21. 변경 범위 관리

작업 시작 전에 가능한 한 변경 범위를 좁힌다.

예:

```text
요청:
공지사항 버튼 문구 수정

좋은 변경:
Index.tsx 해당 버튼 문구만 수정

나쁜 변경:
Index.tsx 전체 리팩터링
+ Button 컴포넌트 수정
+ Tailwind 설정 변경
+ 새 아이콘 패키지 설치
```

작은 문제는 작은 변경으로 해결한다.

---

# 22. 대규모 변경은 먼저 제안한다

다음 변경이 필요하다고 판단되면 바로 실행하지 말고 먼저 사용자에게 이유와 범위를 설명한다.

- 아키텍처 변경
- 새로운 상태관리 시스템 도입
- 새로운 UI 프레임워크 도입
- Router 구조 변경
- PWA 구조 변경
- 배포 방식 변경
- 대규모 파일 이동
- Index.tsx 대규모 분리
- 콘텐츠 저장 방식 변경
- 패키지 대량 업그레이드
- GitHub Actions 변경
- CNAME 또는 배포 경로 변경

제안에는 다음을 포함한다.

1. 현재 문제
2. 왜 변경이 필요한지
3. 변경될 파일
4. 예상되는 위험
5. 더 작은 대안이 있는지

사용자 승인 후 진행한다.

---

# 23. 기존 사용자 변경 보호

작업 시작 시 Git 상태를 확인할 수 있다면 확인한다.

현재 작업과 관계없는 사용자 변경 사항을 발견하면 덮어쓰거나 되돌리지 않는다.

다음 명령을 임의 실행하지 않는다.

```bash
git reset --hard
git checkout -- .
git restore .
git clean -fd
```

사용자의 기존 변경을 삭제할 가능성이 있는 명령은 명시적 승인 없이 실행하지 않는다.

---

# 24. 자동 포맷팅 주의

작은 변경 때문에 전체 파일이 포맷 변경되는 상황을 피한다.

한 줄 수정 때문에 수백 줄 diff가 발생하면 원인을 확인한다.

가능하면 실제 기능 변경과 무관한 formatting diff를 만들지 않는다.

---

# 25. 검증 규칙

코드 변경 후 가능한 경우 최소한 다음을 실행한다.

```bash
npm run lint
npm run build
```

둘 중 실행하지 못한 것이 있다면 완료 보고에서 명확히 밝힌다.

실패했다면 실패 사실을 숨기지 않는다.

기존 오류인지 이번 변경으로 발생한 오류인지 가능한 범위에서 구분한다.

---

# 26. 테스트 스크립트를 임의로 가정하지 않는다

현재 package.json에 없는 명령을 존재한다고 가정하지 않는다.

예:

```bash
npm test
npm run typecheck
npm run test:e2e
```

등이 실제 scripts에 존재하는지 먼저 확인한다.

검증 명령은 항상 현재 `package.json`을 기준으로 판단한다.

---

# 27. 브라우저 확인

UI 변경이라면 가능할 경우 실제 화면도 확인한다.

특히 다음을 확인한다.

- 페이지가 정상 렌더링되는가
- 콘솔에 새로운 오류가 없는가
- 모바일 레이아웃이 깨지지 않는가
- 변경한 버튼/링크가 정상 작동하는가
- Accordion이 정상 작동하는가
- 긴 텍스트가 화면 밖으로 넘치지 않는가
- 기존 FloatingNav가 콘텐츠를 가리지 않는가

자동 검증이 성공해도 실제 UI가 잘못될 수 있음을 고려한다.

---

# 28. 오류 수정 원칙

버그 수정 시 다음 순서를 따른다.

```text
증상 확인
↓
관련 코드 확인
↓
원인 추적
↓
최소 수정
↓
lint
↓
build
↓
가능하면 실제 화면 확인
↓
결과 보고
```

증상만 보고 추측해서 여러 파일을 동시에 수정하지 않는다.

---

# 29. 경고와 오류를 숨기지 않는다

문제를 해결하기 위해 다음 방법을 우선 사용하지 않는다.

- eslint rule 전체 비활성화
- TypeScript 검사 우회
- `any` 남용
- `@ts-ignore` 남용
- 오류를 빈 catch로 삼키기
- console 오류만 숨기기

오류의 실제 원인을 수정한다.

예외적으로 필요한 경우 이유를 코드 또는 작업 보고에 명확히 남긴다.

---

# 30. 과도한 추상화 금지

현재 요구사항보다 미래 가능성을 과도하게 예상하여 구조를 복잡하게 만들지 않는다.

예:

```text
한 곳에서 사용하는 작은 데이터
→ 반드시 별도 global store로 만들 필요 없음

한 페이지에서 사용하는 간단한 UI
→ 반드시 새로운 디자인 시스템 abstraction을 만들 필요 없음
```

현재 문제에 맞는 가장 단순한 구현을 우선한다.

---

# 31. 보안 및 개인정보

코드에 다음 정보를 직접 추가하지 않는다.

- API key
- access token
- password
- private credential
- 개인 식별 정보

`.env` 또는 secret이 필요한 기능이라면 기존 프로젝트의 관리 방식을 먼저 확인한다.

GitHub Actions secret을 코드에 하드코딩하지 않는다.

---

# 32. 작업 중 예상 밖 문제 발견 시

작업 요청과 관계없는 문제를 발견했다고 해서 자동으로 함께 수정하지 않는다.

다음처럼 처리한다.

```text
요청된 문제
→ 수정

추가로 발견한 문제
→ 보고

사용자 승인
→ 필요하면 별도 수정
```

단, 현재 작업을 완료하기 위해 반드시 해결해야 하는 직접적인 blocker라면 이유를 설명하고 최소 범위에서 처리한다.

---

# 33. 작업 완료 보고 형식

작업이 끝나면 길고 추상적인 설명보다 실제 변경 사항을 명확히 보고한다.

다음 형식을 기본으로 사용한다.

```text
작업 완료

변경 내용
- 무엇을 수정했는지
- 어떤 파일을 수정했는지

검증
- npm run lint: 성공 / 실패 / 미실행
- npm run build: 성공 / 실패 / 미실행
- 화면 확인: 완료 / 미실행

유지한 사항
- 기존 기능 중 의도적으로 건드리지 않은 부분

추가 확인 사항
- 발견된 문제 또는 사용자가 확인하면 좋은 사항
```

---

# 34. 실패한 검증도 정확하게 보고한다

예:

```text
npm run lint: 실패

이번 변경 파일에서는 새로운 오류가 확인되지 않았지만,
기존 파일의 lint 오류 때문에 전체 명령이 실패했습니다.
```

처럼 구분할 수 있다면 구분한다.

성공하지 않은 검증을 성공했다고 보고하지 않는다.

---

# 35. 작업 전 계획이 필요한 경우

여러 파일을 수정하거나 사용자 영향이 큰 작업이라면 먼저 짧은 계획을 세운다.

예:

```text
1. 현재 구현 확인
2. 문제 발생 경로 확인
3. 최소 수정
4. lint/build
5. 실제 동작 확인
```

계획보다 실제 코드 확인을 우선한다.

---

# 36. Codex가 특히 하지 말아야 할 것

다음 행동은 명시적 요청 없이 하지 않는다.

```text
"구조가 낡아 보여서 전체 리팩터링했습니다."

"편의를 위해 새 상태관리 라이브러리를 추가했습니다."

"Index.tsx가 길어서 전부 여러 파일로 나눴습니다."

"더 현대적으로 보이도록 전체 UI를 바꿨습니다."

"외부 링크보다 내부 데이터가 좋아 보여서 이전했습니다."

"패키지가 오래되어 보여서 전부 업데이트했습니다."

"코드를 깨끗하게 하기 위해 관련 없는 파일도 수정했습니다."

"사용하지 않는 것 같아서 PDF/이미지/링크를 삭제했습니다."

"배포를 개선하기 위해 GitHub Actions를 수정했습니다."
```

이 프로젝트에서는 이러한 변경이 코드 품질 향상이 아니라 운영 위험이 될 수 있다.

---

# 37. 우선순위

판단이 애매할 경우 다음 우선순위를 따른다.

```text
1. 실제 업무 정보의 정확성
2. 기존 정상 기능 유지
3. 사용자의 요청
4. 모바일 사용성
5. 접근성과 가독성
6. 최소 변경
7. 기존 프로젝트 패턴 유지
8. 코드 단순성
9. 개발 편의성
10. 미관을 위한 리팩터링
```

---

# 38. 최종 원칙

이 저장소의 목표는 코드를 가장 세련되게 만드는 것이 아니다.

목표는:

> 활동지원사가 현장에서 필요한 정보를 빠르고 안정적으로 확인할 수 있는 업무매뉴얼을 유지하고 개선하는 것

이다.

따라서 모든 변경에서 다음 질문을 먼저 한다.

> 이 변경이 사용자의 요청을 해결하면서 현재 정상 작동하는 부분을 가장 적게 건드리는 방법인가?

그렇지 않다면 더 작은 변경 방법을 먼저 찾는다.
